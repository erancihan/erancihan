# Review — unit 2 (02-metal-fundamentals) · pedagogy reviewer · 9/10

Verified: format OK (18% code, no block over 11 lines).

Concept chapter, correctly exempt from the location-line rule. The one change
needed was making the preview status explicit: snippets now say plainly that
they carry no location line *because* nothing is being created yet, so the
reader never wonders whether they missed a file. Object-lifetime table and the
depth-state policy block remain the chapter's payload and are unchanged.
