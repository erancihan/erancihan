# Review — chapter 03 (The math you need)

**Persona:** pedagogy reviewer who checks that every claimed console output is real.

**Verified:** 37.6% code, 18 blocks, largest 17 added lines, 100% anchored, lint
clean. `Math.hpp` and `RenderTypes.hpp` reconstruct **exactly** (24 / 46 code lines).
**Every failure output claimed in the text was reproduced by actually building and
running the naive versions** — the naive projection, the fixed one, and the
`static_assert` with `vec3` members.

**What's right**
- Three derive-don't-present sequences, all on real invariants, all *checkable*:
  depth range (−1.000 → 0.000), the Y-flip (+0.314 → −0.314), and std140 padding
  (`88 == 96` at compile time). The reader writes the wrong version each time.
- `scratch/math_check.cpp` solves a real pedagogical problem: without a renderer
  until chapter 08, a wrong matrix would otherwise be invisible for five chapters.
  Testing in view space isolates the projection from the camera.
- The `static_assert` trio is a permanent improvement to the project, not a
  teaching prop — it converts a silent GPU-reads-garbage bug into a build failure.
- `Vertex` is explicitly *not* padded, with the reason (two layout worlds), which
  pre-empts the obvious over-correction.
- The deliberate breakage is unusually good: flipping `p[0][0]` instead of `p[1][1]`
  makes the harness *pass* its Y check while mirroring X — demonstrating that the
  harness itself is not a proof, which is the honest lesson.

**Findings**
1. *(fixed)* The chapter claimed `y = ±0.633`. Measured value is `±0.314`. Also
   corrected the `static_assert` diagnostic to gcc's real wording
   (`the comparison reduces to '(88 == 96)'`). **This is exactly the class of error
   the "run it" discipline exists to catch** — a plausible invented number.
2. *(fixed)* Quaternion-operations block needed an illustration marker.
3. `RenderTypes.hpp` is created here rather than in the buffers chapter. Justified:
   `Components.hpp` (ch04) needs `MeshID`, and this is the chapter that teaches the
   padding rule the structs embody. Chapter 06 revisits them with the VMA detail.

**Score: 9.5/10**
