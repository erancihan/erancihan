# Review — chapter 10 (Flight & input)

**Persona:** gameplay engineer who has shipped a left-multiplied quaternion.

**Verified:** 34.1% code, 14 blocks, largest 17 added lines, 100% anchored, lint
clean. `Input.hpp` and `FlightControlSystem.hpp` reconstruct **exactly**;
`main.cpp` now matches canonical in full. Reconstructed tree builds, runs
validation-silent on WSLg/lavapipe, and — the observable claim — the ship that sat
motionless at ch09's checkpoint now pulls away at cruise speed, confirmed by
before/after screenshots (ship at centre → distant speck at horizon).

**What's right**
- The right-multiply is handled as a *described* derivation with a precise,
  reproducible symptom: change to `delta * t->rotation`, feels identical flying
  level, roll 90° and pull `S` — ship yaws instead of climbing. "The difference
  between an aircraft and a security camera." Reader performs it in the breakage
  section rather than the main path, which is the correct weight given the guide
  already carries eight full derivations.
- `+=` on axes justified (opposite keys cancel); physical-position key tokens;
  pitch-sign convention named as a convention with the one-line invert option.
- The ch04 dangling-pointer promise is *serviced honestly*: this chapter's
  cross-store `set` is explained as safe and why, with the same-store hazard
  deferred to ch12 where it actually bites.
- Schedule-position rationale (flight before movement, or input lags one frame,
  "subtly gluey forever") is the kind of thing that never gets taught.
- Challenge names three real gamepad traps: deadzone drift, GLFW's Y-down axes,
  and the keyboard/pad merge policy.

**Findings**
1. **Verifier caught duplicated includes** in `main.cpp` — ch09's include-replace
   diff had anchored below two stale lines, so VulkanContext/Swapchain/<exception>
   appeared twice. Compiles silently (include guards), so only the exact-match
   comparison could see it. Fixed ch09's diff.
2. *(canonical reconciled)* Removed a redundant `vkDeviceWaitIdle` from canonical
   `main.cpp` (`Renderer::destroy` already waits); kept the `[swapchain]`
   diagnostic print permanently.

**Score: 9.5/10**
