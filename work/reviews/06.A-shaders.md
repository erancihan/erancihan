# Review — unit 6 (06.A-shaders) · pedagogy reviewer · 9/10

Verified
- Format OK: 39% code, largest block adds 20 lines (was part of a 120-line paste).
- Reconstruction: Shaders.swift replays exactly from 8 anchored steps.

Strengths
- Opens with the four MSL ideas (pull-vertex, instance_id, buffer-index contract,
  separate per-stage binding tables) so every later attribute is already named.
- The duplicated struct declarations are confronted rather than glossed: this is
  the same declaration twice, it must stay byte-compatible, and here is what a
  bigger project would do instead.
- lit_vertex's normal-matrix line gets the treatment it deserves — why the 3x3,
  why the inverse transpose is normally required, and exactly when our uniform-
  scale shortcut becomes the bug.
- The star wrap is framed as a transferable habit (move the rule into the shader)
  rather than a clever trick.
- discard_fragment carries its real-world caveat about early-Z.
- Honest checkpoint: says plainly that nothing runs yet, then gives a brace/entry-
  point sanity check that catches the one painful failure (an unterminated string
  swallowing the file).

Concerns (accepted)
- Chapter ends without a visual payoff; unavoidable when splitting 06, and the
  intro sets that expectation explicitly ("building the far side of a bridge").
