# Review — chapter 02 (Vulkan fundamentals: instance, device, queues)

**Persona:** pedagogy reviewer + Vulkan practitioner.

**Verified:** 48.5% code, 33 blocks, largest 19 added lines, 100% anchored, lint
clean. `VulkanContext.hpp`, `VulkanContext.cpp` and `VmaImplementation.cpp` all
reconstruct **exactly** against canonical (40 / 187 / 3 code lines).

**What's right**
- The chapter now *builds and runs*. The original ch02 was a concept chapter whose
  code went nowhere; this one ends with the program naming your GPU.
- **Derive-don't-present lands here** on a genuinely non-obvious invariant: the
  debug messenger cannot see `vkCreateInstance`, because it is created *from* the
  instance. The reader writes the naive version, is shown exactly what it costs
  (a numeric failure code and silence at the worst possible moment), and then
  refactors to `fillDebugCreateInfo` + a `pNext` chain. The `dbg` lifetime trap is
  called out where it bites.
- Real reasons attached to lines that look like boilerplate: `std::optional` over
  `-1` because family 0 is valid; the `std::set` because asking twice for one
  family is invalid usage; `GLFW_NO_API` because the default gives you a stray GL
  context; `VmaImplementation.cpp` because 20k lines shouldn't recompile.
- Checkpoint promises *silence* as the pass condition and explains why, plus a
  6-row symptom table and a deliberate breakage (reorder `destroy()`, read the
  layer's object-lifetime complaint, put it back).
- The "why so verbose" section keeps the original's honest counterpoint.

**Findings**
1. *(fixed)* An illustration block and one 22-line block; marker moved into prose,
   the `main.cpp` block split at the try/catch seam — which improved it, since the
   error-handling rationale now has its own paragraph.
2. 48.5% code is the highest so far but under the 55% bar, and it is the nature of
   the chapter: this is where Vulkan's create-info verbosity is densest. The
   average block is 9.9 added lines, so the density is spread thin, not lumped.

**Score: 9.5/10**
