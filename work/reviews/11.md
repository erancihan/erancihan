# Review — chapter 11 (The camera)

**Persona:** game-feel reviewer.

**Verified:** 23.3% code, 5 blocks, largest 20 added lines, 100% anchored, lint
clean. `CameraSystem.hpp` reconstructs **exactly**; `Game.cpp` differs only by
ch12/13 additions. Reconstructed tree builds, runs validation-silent; screenshot
shows the promised composition — ship lower-middle, over-the-shoulder rig, grid
receding below.

**What's right**
- The up-vector blend is taught by its two failure modes ("detached" vs "washing
  machine") with a 30-second experiment for each, and the normalize on the blend
  gets its actual reason (short up-hint near forward degrades lookAt precision).
- "One flip, one place" — the chapter explicitly confirms the *absence* of any
  Vulkan correction here and explains why adding one would cancel the projection's.
- `kLookAhead` earns a deliberate breakage that is about *feel*: set to 0, fly a
  slalom, feel information arriving late. Correct weight for a chapter with no new
  invariants.
- Near/far taught as a precision *budget* with the classic `nearZ = 0.001` mistake
  named. `cameraPosition` finally earning its uniform slot (star re-tiling) ties
  three chapters together.
- Fly-through-the-floor is addressed head-on as scenery-not-geometry rather than
  left for the reader to report as a bug.
- Challenge names the statics trap: a function-local `static` for smoothing state
  silently breaks the "point it at any entity" property.

**Findings**
1. None from the checker — clean first pass.
2. Chapter is the shortest build chapter (236 lines) and that is right; padding it
   would dilute the two ideas it owns.

**Score: 9.5/10**
