# Review — chapter 13 (HUD & feedback)

**Persona:** UI-adjacent graphics reviewer.

**Verified — and this chapter closes the loop:** after it, the full verifier
reports **40 of 40 files reconstruct exactly** against canonical, lint clean.
The tree rebuilt from the chapters alone builds and runs; a 23-second unattended
session ended at `score 200  hull 80%  deaths 0` in the title, and the screenshot
shows every element working: cyan reticle dead-centre with square arms, hull bar
drained to 80% with the dark background exposed on the right (left-anchored drain
confirmed visually), bolts in flight, an amber drifter inbound. Validation
silent.

**What's right**
- The render-side contribution is six lines, and the chapter says *why*: buffer
  (ch06), pipeline (07.B), upload/clamp (08) were all cut at the right seams and
  have been waiting. That retrospective is the architecture lesson of the guide.
- The hull bar teaches two non-obvious details as decisions: the computed
  three-stop colour gradient, and the moving-centre expression that pins the
  fill's left edge (with "shrinks from both ends" as the symptom of simplifying
  it away).
- Aspect correction applied to the reticle and deliberately *not* to the bar,
  with reasons for both.
- The deliberate breakage (flash moved to the end of `build`) demonstrates that
  painter's order is an interface decision — the red wash hides the hull bar at
  the exact moment the player needs it.
- The text section is honest about the atlas option's real cost (first texture =
  first sampler = first image descriptor) rather than waving at it.
- Challenge (edge indicators) names the negative-`w` reflection trap — the one
  that makes indicators point exactly wrong for enemies behind you.

**Findings**
1. *(canonical reconciled)* `CMakeLists.txt` source order — canonical listed
   Game before Mesh; the chapters add them in build order (Mesh ch08, Game
   ch09). Reordered canonical; this was the last file short of 40/40.
2. No other checker findings.

**Score: 9.5/10**
