{
  "name": "aether-downloader",
  "version": "1.0.0",
  "description": "Unified Pixiv & Zerochan image downloader Firefox extension",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node build.js --watch",
    "build": "node build.js",
    "package": "npm run build && web-ext build -s dist -a artifacts --overwrite-dest"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "tailwindcss": "^4.1.8",
    "@tailwindcss/vite": "^4.1.8",
    "web-ext": "^8.4.0"
  }
}
