{
  "name": "antigravity-parallel-agents",
  "version": "0.0.0",
  "private": true,
  "description": "Fan out a batch of tasks across many Antigravity agents in parallel, monitor the lanes, and aggregate results.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "keywords": ["antigravity", "gemini", "agents", "parallel", "orchestration"],
  "license": "MIT"
}
