version: '3.8'

services:
  expense-app:
    build: .
    volumes:
      - ./data:/app/data
      - ./secrets:/app/secrets
    environment:
      - CHECK_INTERVAL_MINUTES=60
    restart: unless-stopped
