# Chapter 10 review — round 1

Reviewer persona: IEEE 754 specialist; every number reverified against an independently built exact single-rounding oracle. All measurements in this review were produced this session with the reviewer's own code and seeds unless explicitly attributed.

<!-- sections complete: 10/10 -->

## Verdict

**8.5 / 10 — every number the chapter prints survived independent re-derivation; a short list of small but real record/wording defects keeps it from 9.**

I rebuilt the exact single-rounding oracle from scratch (integer arithmetic + hand RNE, validated on 380,576 checks against the struct/double safe-harbor oracle, zero mismatches), re-measured every headline with my own seeds and generators, re-ran every shipped bench, rebuilt and re-ran the load-bearing mutations myself, ran my own novel attacks, and drove a 200,000-quadruple three-way RTL sweep (both DUTs vs `ref_add4` vs my oracle) that passed clean. Specifically confirmed: the 0.65/23.99/30.11 census (mine: 0.65/24.01/29.98, plus a byte-identical 240k in-TB rerun); the 1.32/20.46/21.51 false-oracle rates (mine: 1.41/20.32/21.87); the 7.095 % one-directional inexact over-report (mine: 7.112 %) **including a 900,000-evaluation adversarial hunt for the impossible direction that found nothing, and an independent verification of the two-line proof**; the 1.27→28–33 % chained-reference cost; the no-accuracy-winner claim in exactly the form the chapter states it (significant per-regime differences whose direction flips — win10 to seq, win2 to tree — never a systematic winner); the full {max,max,−max,−max} three-answer walk by hand; every corner constant; the 1,369 = census identity for the order-unfaithful reference; D6's 3,152 kills; X1's 6,145/6,159/14-more false pass; and D9's survival with a structural proof I verified against the actual screen/mux RTL.

What keeps it from 9: STATE.md's ch10 bullet overclaims "statistically indistinguishable" where the truth (and the chapter's own text) is "significant but direction-unstable" — the project's recurring defect class, now sitting in the record later chapters quote; the README's D4 evidence line is contradicted by the author's own stored log; one positional cross-reference in the chapter is factually wrong ("two sections below" is four); and the corner suite prints, but does not assert, the N4/N5 NaN payloads its own table lists as expectations. All fixes are a few lines each; the required-changes list is ordered and concrete.

## Numbers reverified

**Method.** I built my own exact single-rounding oracle from scratch — integer scaled-significand arithmetic (M·2^q), hand-written RNE encoder, subnormal/overflow/exact-zero handling — sharing no code with the project's `fp32lab`/`f4.py`. Validated before use: 380,576 checks against the struct/double innocuous-double-rounding one-add oracle (directed corners including subnormal boundaries, ties, near-overflow; three exponent regimes; subnormal-heavy pairs; `cr4`-with-zero-pads pairwise consistency) — **zero mismatches**. All re-measurements below use my own seeds (777001, 555002, 31337, 424242) and my own re-implementation of the chapter's stated generators.

**Association census (claimed 0.65 / 23.99 / 30.11 %).** My 1.2M-quadruple sweep: **0.65 / 24.01 / 29.98 %**. Confirmed. The in-testbench census also reproduces byte-for-byte: the shipped `tb_equiv4` at `-DNQ=60000` printed `full=402 win10=14403 win2=18003 specialmix=342` in 5m46.8s this session — identical to the chapter's quoted transcript (and its "5 m 47 s").

**Accuracy vs CR (claimed: full 1.63/1.63, win10 28.05/27.99, win2 31.74/33.09 % ≠ CR; no systematic winner).** Mine: full 1.66/1.64, win10 28.06/28.10, **win2 31.74/33.07** — the win2 row lands within 0.02 points of the chapter's on a different RNG. Step histograms, the 98.37 %/99.96 % full-range claims, and the 98.7 %/96.3 % within-two-steps claims all check out against the chapter's own table (and my histograms are statistically consistent). Strictly-closer direction flips exactly as claimed: my full = seq +2.2σ (theirs ~tie), my win10 = seq (n.s. in my draw; significant in theirs), my win2 = **tree at +10.3σ** (theirs equivalent). McNemar on ≠CR bits: win2 favors tree at ~17σ, win10 n.s., full marginal seq. So the chapter's precise claim — significant per-regime differences whose **direction flips between regimes**, dwarfed by between-regime effects — is correct as written. One caveat lands on STATE.md, not the chapter: its bullet "disagreement-with-oracle rates **statistically indistinguishable**" is false (win2's 31.74 vs 33.07 % on 400k quadruples is a >13σ difference in both my run and theirs). The chapter avoided the overclaim; the project record did not. See Required changes.

**False oracle (claimed 1.32 / 20.46 / 21.51 %).** Mine, own seed: **1.41 / 20.32 / 21.87 %**. Confirmed within sampling error, and the author's stored `agree.log` carries the exact claimed values.

**One-directional inexact (claimed 0 / 0.362 / 7.095 %, reverse impossible).** Mine: 0.001 / 0.370 / **7.112 %**, all forward. I then hunted the reverse direction (composition exact while the true sum is inexact) adversarially: 300,000 extra quadruples × both structures built to maximize all-stages-exact outcomes (sparse fractions at 1–4 random bits, ±2 windows, engineered >24-bit exponent gaps, near-tie x/x±ulp patterns) — **zero hits in ~900,000 structure-evaluations**. The chapter's two-line proof is sound and I verified its logic independently: three exact stages force the composed result to equal the exact four-operand sum, which is then representable, so CR's inexact is 0. The one-directionality claim survives the hardest attack I could design. (My own full-regime draw found 1 forward case where the chapter's seed found 0 — the chapter presents 0 as a measurement, not an impossibility, so no conflict.)

**Chained-unrounded-reference cost (claimed ~1.27 / 28.1 / 31.6–33.0 %).** Mine: tree 1.333 / 27.96 / 31.86 %, seq 1.334 / 28.07 / 33.21 %. Confirmed; also consistent with ch05's seeded 1.298/1.295 % full-range figures, which I checked against ch05's actual text.

**{max, max, −max, −max} walked by hand.** I worked all four stages of both structures and both port orders with pencil-level IEEE semantics and my oracle: tree ports-as-listed → +inf/−inf at level 1, inf+(−inf) → **qNaN, inv+ovf+inx (111)**; chain → **+inf, 011** (the second −max is absorbed, invalid never fires); interleaved → exact cancellation at every stage, **+0, 000**. CR(Q1) = +0, CR(Q2) = 7F7FFFFF (max, representable — lost to range, not rounding). All three outcomes and every flag match the chapter's ledgers and the RTL transcript I captured.

**Every corner expectation re-derived.** Q1–Q3, N1–N5 (including 7FA00000 → 7FE00000 quieting with payload kept and the N4 invalid-must-stay-0 rule), all 16 signed-zero combinations (−0 iff all −0), Z1's cancellation-generated +0, S1 (tree +0 / seq 1.0 / CR 2.0 — including the −(2²⁵−1) tie-away-to-−2²⁵ step), S2 (tree 4B800001 / seq 4B800000 / CR 4B800002 tie-to-even), SB's subnormal chain — every value and flag triple re-derived independently and confirmed against my oracle and the RTL. The four reacher families' halving/quarter splits are exact by construction (verified), and the R0–R3 table rows match `tb_reach4.v`'s arrays.

**Directed wild examples.** `DC44714B 5C3955A1 DBF86B93 5C0749DD`: tree = D5F2D000 = CR, seq off exactly 4,096 steps; mirror `7C8D49C8 7D4A751E FD655607 FC2F3DA8`: seq = F736F000 = CR, tree off 4,096; cancellation magnitude 12.7 binary orders — all three claims verified with my oracle. The win2 outlier `FB284946 FB17B457 7AC276BF 7B5EC2A8`: both structures 72D80000, CR 72D50000, 196,608 steps each — verified. The inexact example `464F7804 47B381F1 466DF600 C6A45DF6`: stage pattern 1,0,1, final 47C21834 = CR, true sum exact — verified.

## RTL and reference audit

**Structures.** Both wrappers audited line by line: `fp32_add4_tree` and `fp32_add4_seq` are exactly three unmodified `fp32_add2` instances plus three 3-input OR assigns each — no hidden glue, no width games (`sum_ab`/`sum_cd`/`sum_abc` all `[31:0]`), `default_nettype none` both ends. Port mapping matches the declared associations `(a+b)+(c+d)` and `((a+b)+c)+d` exactly. The header comments' claims (Q1/Q3 behavior, no-accuracy-winner, flag rule) match the measurements.

**ref_add4.v.** Audited hard, since it is the chapter's chained-reference teaching: `ref_add4_tree` wires three ch08 `fp32_add_alg` instances in precisely the tree's order (r_ab: a,b; r_cd: c,d; r_r: s1,s2) and `ref_add4_seq` in precisely the chain's; flags ORed identically to the DUTs. Round-tripping is inherent in the style — every interior edge is a packed 32-bit port. NaN-by-class is handled where it belongs (the style-B comparison in `tb_equiv4`); style A legitimately asserts NaN bits because identical modules see identical stage inputs (ch09's 200,092-check bit-identity, quoted correctly). Independent validation: my **three-way sweep** — 200,000 quadruples, my own seed and generators (four regimes including a special mix with sNaN/qNaN delivered as bits), file-driven expectations from my own oracle — compared each DUT against `ref_add4` (result + all three flags, `!==`, X-guarded) *and* against my python exact-path per-stage model (result bits or NaN class, plus all three OR flags): **PASS, zero mismatches in 200,000 × 2 structures × 2 comparisons.** That closes the loop the chapter's own campaign leaves implicit: `ref_add4` agrees with an oracle built from nothing but IEEE 754 and integer arithmetic.

**Cross-chapter wiring claims.** The manifest's dependency story checks out: three targets, all compiling the full ch02/ch07/ch09 source set, zero `iverilog -g2012 -Wall` output (verified on every compile I ran), `run_all.sh ch10` 3/3 green, and the harness's `SIM_TIMEOUT=60` matches the chapter's "60 s timeout sizes it". The shipped 24,000-quadruple sweep ran in 37.8 s on this container against the claimed 34.5 s — same-container run variation, still inside the timeout. Cited constants verified at their sources: ch09's cut widths 100/95/73/72 (`fp32_add2.v` header and ch09 tables), the 1,761-gate sizer inventory (ch09 line 377), ch08's 46-pair library and triple census 0.64/21.79/32.41, ch05's 1.298/1.295 % seed callout and ~288,000 tx/s figure, ch09's 100,092-vs-200,092 check counts (ch10 uses each in the correct place).

## Mutation record

I designed my own attacks before reconciling with the README, then reproduced the record's load-bearing rows from scratch (all mutants rebuilt by me in the scratchpad from the shipped sources; nothing under `guide/src` touched).

**My own attacks (not in the README) — all caught:**

| attack | result |
|---|---|
| tree cross-wired `(a+d)+(c+b)` — a different permutation than D1's | KILLED: 4,025 tree-side errors in the probe (2,106 golden + 1,919 shortreal), 0 seq-side |
| one OR term dropped (`inexact = inx_ab \| inx_r`, `inx_cd` lost) — subtler than D3's full drop | KILLED: 613 flag-only kills, **all via the golden chain's flag compare, 0 via the flag-blind shortreal reference** — independently confirms the chapter's claim that style A's flag comparison is load-bearing |
| order-unfaithful **style-A** reference (ref_add4_tree rewired to seq order) vs the correct DUT | KILLED: **1,693** false failures — more than T1's 1,369, because a flags-carrying reference also diverges on bits-agreeing quadruples. See Required changes for the one wording this sharpens |

**README rows reproduced exactly.** D1: 4,135 tree-side, 0 seq-side. D2: 2,703 seq-side with the identical first-kill line (`bd8fd94f 8a7b5ad6 c460b7b2 ca46b8c2 dut=ca46c6cd/001 ref=ca46c6ce/001`). D3: 12 corner flag-only fails (same 12 lines), 3,935 equiv kills all via golden-chain flags, 0 via shortreal. D5: exactly 2 "Padding 1 high bits" warnings, 7,788 equiv kills, first kill `dut=7bc6d0a8` for `fbc6d0a8` (sign lost), corners Z k=1111 kill. D6: **3,152 kills = 785 tree + 791 seq via the golden chain AND the same counts again via shortreal** — byte-exact to the claim. D7: 9 tb_reach4 fails, `00000000` for `4b800000` signature, fire-count guard 0/0. D8: 8 kills in the probe, all dual-NaN special-mix quadruples (7FA00000/7FC00000 pairs), golden-chain-only, shortreal blind — confirmed. T1: **exactly 1,369 seq-vs-shortreal failures against the correct design, 0 of any other kind — and I re-derived the census independently: the probe prints `full=17 win10=607 win2=731 specialmix=14`, sum 1,369.** T2: reconciled at exactly 1,562 with a tree-shaped double sum `(sx+sy)+(sz+sw)`; my first attempt with a left-to-right chain gave 1,565 — the count is mildly sensitive to the double-sum association, worth one clarifying word in the README but not an error. T3/T4/T5-equivalent/T7/T8: all gates fire with the exact quoted messages (`check count 0 (expected 0, nonzero)`; empty-bin gate with `ovf_s1 t=0 s=0`; `fires tree=0 seq=4`; elaboration error naming `round_renorm_q`).

**X1, reproduced fully.** I ran the scratch `tb_cross4.v` against the correct design and against D6: `PASS ... 6145 cross-checked` and `PASS ... 6159 cross-checked` — byte-identical to the chapter's two transcript blocks, and I verified the "14 more" mechanism independently: the cross-checked count is fully determined by the shared `$urandom` stream (7,500 − the mutant's own tree≠seq census 13+576+752 = 6,159), so the broken design provably agrees with itself more than the correct one (6,145) on this stimulus. Full-range agreement 99.32 % ≈ the claimed ~99.3 %. Meanwhile the same mutant dies 3,152 times under `tb_equiv4`. The chapter's centerpiece demonstration is genuine.

**D9, the survivor, scrutinized.** I rebuilt the mutant (`exact_zero = (sum27 == 27'd0)`, gate dropped) and ran the entire kit: shipped 24,000-quadruple sweep, all 28 corners, the reachers — **all PASS**, as recorded. The structural proof is sound, and I verified it against the actual RTL rather than taking the prose: on an effective add, `sum27 = {0,sig_big,00} + {0,aligned,g,r}` in 27 bits with no wrap possible, so `sum27 == 0` requires `sig_big == 0`, which only a zero big operand produces — and the big operand is the larger magnitude, so both operands are zero; `fp32_screen` raises `screen` on any zero operand and the top's mux (`result = screen ? screen_res : dp_res`, flags `~screen`-gated) discards the datapath entirely. The mutant is therefore *observationally equivalent at fp32_add2's ports for every input*, at any composition depth — the mask travels with the module, exactly as claimed. Consequence for the erratum question: this is **not** a ch09 erratum (the shipped RTL has the gate; the "bug" is a planted mutant violating the sub-module's contract without violating any composed output), and no open concern needs filing beyond what the chapter and README already record. One sharpening available: since the mutant is provably output-equivalent, "unkillable by any port-level campaign" is a theorem, not an empirical survival — the README's "provable at any campaign size" already says this; good.

**D4, the one record error found.** The README's evidence reads "5 flag fails, first N1 tree `001` for `100`". The kill count is right and the mechanism is right, but the *first* fail is **Q2 (`110` for `011`)**, then N1 — in my fresh run of the identical mutant *and in the author's own stored log* (`mut10/d4c.log` shows `FAIL Q2 tree flags` before `FAIL N1`). The README mis-summarizes its own evidence. Small, but this record exists to be exact.

## Testbench honesty

Audited all three shipped benches line by line and attacked their guards (see Mutation record for the guard-firing runs).

- **`!==` + X-guards**: every comparison in all three benches is `!==`; `tb_equiv4` and `tb_corners4` fail on `^result === 1'bx` before comparing; `tb_reach4` folds the X-guard into each check. Flag X-contamination is caught by the `!==` flag compares even without a dedicated guard. Sound.
- **Watchdogs**: all three are time-based (`#600_000_000` / `#1_000_000`) with `$fatal(1)`, ample headroom at the shipped sizes, never counting events. Sound.
- **Count guards**: `tb_equiv4`'s is preconditioned (`checks == 0 || checks !== 4*NQ`) and I verified `-DNQ=0` dies on it; `tb_corners4` pins 28, `tb_reach4` pins 9 checks *and* 4/4 fire counts (both shown able to fire).
- **Seeding**: seeded once through an integer variable, first draw discarded, matching the ch05 linearity rule; a literal seed is indeed rejected at vvp load (ch04 finding, quoted correctly). Modulo exponent draws' bias ≈ 254/2³² ≈ 2⁻²⁴ — "stated not hidden" is accurate.
- **Coverage bins**: the ten intermediate-event bins gate on empty (T4/T5 reproduced), hierarchical paths fail elaboration when stale (T8 reproduced), and the fire-count guard catches the re-aimed-but-existing path (T7 reproduced). The `isfinnz` guard on the cancellation bins does real work — I traced Q3-tree (screened zero operands at the final stage) and confirmed it would otherwise pollute the bin; with the guard, cancel_tree counts only S1 and cancel_seq only Q3, exactly as the chapter narrates. The printed bins line `ovf_s1 t=2 s=2 nan_s1 t=3 s=2 nan_late t=2 s=2 cancel t=1 s=1 sub_s1 t=1 s=1` reproduces exactly and each count is individually explainable (I re-derived all ten).
- **Reachers under composition**: `tb_reach4`'s halving/quarter splits are exact-arithmetic-verified (every intermediate a genuine computed sum: hx+hx = X exactly, qx+qx = hx exactly); the white-box wires `dut_t.u_add_r.u_round.round_renorm` / `dut_s.u_add3.u_round.round_renorm` are the right final-stage rounders; 4/4 fires in both structures plus a negative control at 0 — all re-run and confirmed.
- **One honesty gap, small but real**: the chapter's regression table lists exact expected bits for N4 (`7FC00001`, payload intact) and N5 (`7FE00000`, payload kept + quiet bit), and the chapter's text leans on "propagates with its payload intact ... which is precisely why it is in the suite" — but `tb_corners4`'s NaN kind checks **class + quiet bit only**; the payloads are printed, not asserted. The bench documents this honestly in its own comment, and payload discipline is genuinely enforced elsewhere (the golden chain asserts NaN bits across 240k vectors, and D8's kills are payload-order kills), but the directed suite does not enforce the two payload claims its own table makes. Asserting bits[21:0]+quiet on N4/N5 is safe here — these are *propagated operand* NaNs, deterministic per the RTL's screen, not generated NaNs (whose sign the project rightly refuses to compare). Fix is three lines.

## Transcripts, listings, craft

- **Listings**: all 4 Verilog fences in the chapter verified mechanically as byte-identical contiguous slices of the shipped files (tree wiring, seq wiring, `rt_add`, the bins snippet).
- **Transcripts**: every quoted run line re-captured this session and matched byte-for-byte: the Q1/N1–N5/S1/S2 corner lines, the bins line, both quoted reach fire lines (correctly identified as R2's two, abridgement declared), the probe census, the 240k headline (`full=402 win10=14403 win2=18003 specialmix=342`, PASS 240000, 5m47s), both tb_cross4 PASS lines, and the naive-cost figures (author's stored logs also carry them; my own-seed reruns agree statistically).
- **Cross-references**: every quoted section title resolves — ch05 "The Reference Model" / "The Green Run That Means Nothing", ch07 "shortreal: The Bit-Level Laboratory", ch08 "Order Is Part of the Answer" / "Why the Shortcuts Fail" / "How Production Adders Rearrange It" / "Round, and the Renormalize Random Never Finds", ch09 "Cutting Along the Seams" / "Composition, and the Golden-Equivalence Sweep" / "Masking, Measured in Both Directions" / "What Cannot Be Known Without a Synthesizer". T1/T2 pair labels exist in ch08 and mean what ch10 says they mean. **One positional cross-reference is wrong**: "vectors worked two sections below" (in "Accuracy Against the Correctly Rounded Sum", point (c)) — the inter-pair-cancellation vectors are worked **four** sections below. Three other relative references ("the next section" ×2, "two sections away") currently resolve but are the fragile form the ch02-r3 review banned; the descriptive-title forms ("the flags section", "the regression section") are fine.
- **Word count**: `wc -w` = 9,534, exactly as recorded. 18 `##` sections as claimed.
- **Epistemic walls**: the fused/Kulisch/augmented-ops section is measurement-free and flagged as documentation, with the augmented-operations claims correctly attributed to the fetched Wikipedia article and the normative text deferred to the standard; the no-synthesizer wall is restated with ch09's sizer caveats intact. Sources split fetched-vs-title-only accurately (2 fetched URLs; the rest by author/title/venue; no sunburst URLs).
- **Forward handoffs**: ch11 (2S vs 3S + 3S×32 balancing bits, cut points 100/95/73/72, ~700 quadruples/s testbench cost, the accumulator third option) — all verified or correctly quoted; ch12 spec handoff is explicit and complete (tree shape AND port-to-operand mapping AND flag-OR rule AND reference discipline, plus permutations-as-distinct-vectors); ch14 seam (fused/Kulisch/2Sum with S1/S2 as worked examples) consistent with the notes.
- **Closing checklist**: audited item by item — all ten bullets are backed by measurements I independently reproduced; none overclaims.
- **Nit**: "the naive model ... never rounds an intermediate" — it rounds intermediates to *binary64*; "never rounds an intermediate to binary32" is the exact statement. The surrounding text ("more accurate than binary32") is correct.

## Research-note audit

Spot-checked eight note claims against my own measurements and the stored logs; audited chapter-vs-notes in both directions.

1. §5 census table (0.65/23.99/30.11 vs triples 0.64/21.79/32.41) — confirmed by my sweep and ch08's actual text. ✓
2. §11/E9 naive-reference costs (1.268/28.131/31.613; 1.262/28.000/33.020) — confirmed statistically by my own-seed rerun and exactly by `naiveref.log`. ✓
3. §6 flag-divergence (0/0.362/7.095, one-way, 2-line proof) — confirmed by my rerun, my adversarial hunt, and `flagdiv.log` (which also carries the chapter's concrete example verbatim). ✓
4. §6 order-agreement-vs-CR (1.32/20.46/21.51) — confirmed (my rerun + `agree.log`). ✓
5. §12/E8 reacher composition and the R0–R3 quadruple table — confirmed by RTL rerun; splits exact by construction. ✓
6. §4 equivalence sweep (240,000 × 2 × 2, zero mismatches; census 402/14403/18003/342; ~680 quadruples/s probe) — headline rerun byte-identical; throughput consistent (my probe ~15 s, their 14.7 s, README 14.3 s — same-session drift, all order-of-700/s). ✓
7. §3 validation (620,400 checks, zero mismatches) — `validate.log` exists and finishes in 17.2 s; independently, my own 380,576-check validation of a from-scratch oracle agrees with everything both models predict. ✓
8. §7–8 corner ledgers — all re-derived (see Numbers). ✓

**Notes-vs-chapter deltas found (both minor, notes-side staleness):** (a) the notes say `tb_corners4.v` has **27** directed checks (artifacts table and §12) and their §12 corner table has no SB row — the shipped bench and chapter have **28** checks including SB; the notes were not updated when SB was added. (b) The notes' §13 cites Tao et al. 2012, which the chapter's sources dropped (legitimate pruning, no contradiction). No silent contradiction of the chapter by the notes or vice versa beyond (a).

## Required changes for a 9+

Nothing here is blocking (no wrong headline number, no false one-directionality, no unkillable bench, no reference defect — I attacked all four and they held). In priority order:

1. **STATE.md, ch10 findings block — fix the overclaim.** "no systematic accuracy winner ... (1.2M-quadruple sweep vs the exact oracle; disagreement-with-oracle rates **statistically indistinguishable**)" is false: in win2 the tree's 31.74 % vs the chain's 33.09 % on 400k quadruples is a >13σ difference (McNemar ~17σ in my rerun), and the strictly-closer counts differ at ~10σ. The chapter's own formulation is the correct one — replace the parenthetical with it, e.g. "per-regime differences are real but small and their *direction flips between regimes* (win10 favors seq, win2 favors tree), so neither structure is systematically closer." This is the project's recurring defect class landing in its canonical findings list, where ch11/ch12 will quote it. *Fix: one sentence in STATE.md.*
2. **README mutation record, D4 row — correct the evidence line.** "first N1 tree `001` for `100`" is wrong: the first fail is Q2 (`110` for `011`), in my fresh run of the identical mutant and in the author's own stored `d4c.log`. Either quote Q2 as first or list the 5-fail set {Q2, N1, N5, S1, S2}. *Fix: one table cell.*
3. **Chapter, "Accuracy Against the Correctly Rounded Sum" (c) — fix the wrong positional cross-reference.** "vectors worked two sections below" → the inter-pair-cancellation section is *four* sections below. Use the quoted title ("worked in 'Inter-Pair Cancellation, Both Directions'"), per the standard the ch02-r3 review set. While there, consider converting the two "the next section" instances and "two sections away" to title form; they resolve today but are the fragile pattern. *Fix: one phrase (plus optional three).*
4. **tb_corners4 — assert the N4/N5 payloads the chapter's table promises.** The table lists exact bits (`7FC00001`, `7FE00000`) and the text says "payload intact ... precisely why it is in the suite," but the bench checks class+quiet only. These are propagated-operand NaNs (deterministic; the generated-NaN sign rule does not apply), so asserting `res[22:0]` — or bits[21:0] plus the quiet bit — is safe and closes the gap between the table and what is enforced. Alternatively, footnote the table "class+quiet asserted; payload shown". *Fix: ~3 lines of bench or 1 footnote; if the bench changes, re-run and update the record.*
5. **Research notes — update the corner count.** Two places still say 27 directed checks and the §12 table lacks the SB row; the shipped suite is 28 with SB. *Fix: two numbers, one table row.*
6. **Chapter, four-rules paragraph — scope "no more, no fewer".** "an order-mismatched reference fails a correct DUT on precisely the quadruples where the two associations differ, no more, no fewer" is exact for the *value-domain* (shortreal) reference (T1: 1,369 = census, reproduced). My style-A variant of the same mutation fails **1,693** — flags diverge on bits-agreeing quadruples too. Add "for a result-bits reference" (or equivalent) so rule 2's price tag isn't quoted against a flags-carrying reference. *Fix: four words.*
7. **Nit, same section as trap 4**: "it never rounds an intermediate" → "never rounds an intermediate *to binary32*" (it rounds three times in binary64). *Fix: two words.*
8. **Nit, README T2 row**: state the double-sum association used ("tree-shaped `(a+b)+(c+d)` in doubles") — the kill count is 1,562 tree-shaped vs 1,565 left-chained, so the row is only exactly reproducible with the shape stated. *Fix: one clause.*

## Tree restoration proof

All mutation and scratch work was done exclusively on copies under the session scratchpad (`scratchpad/rmut/`, `scratchpad/rtl/`, scratchpad python files); no file under `guide/src` was ever edited. Proof, run after all review work completed:

- `sha256sum -c baseline.sha256` (163 files, snapshotted from `guide/src` before any review work): **163/163 OK, zero mismatches, exit 0**.
- `git status --short` at the repo root: the only entry is `?? guide/reviews/ch10-review.md` — this review file itself. No modification, deletion, or addition anywhere else.
- Re-run of the shipped harness after the review battery: `bash run_all.sh ch10` → 3/3 PASS (and the full-tree regression was untouched; the author's stored `full_regression.log` shows 95/95, re-confirmed unmodified by the checksum pass).

## Post-fix verification — 2026-08-20, same reviewer

All eight required changes verified at their sites, re-run, and re-attacked where code changed.

1. **STATE.md bullet** — now reads "per-regime differences are real but small and their *direction flips between regimes* — win10 favors sequential, win2 favors tree — so neither structure is systematically closer," with a dated correction naming the >13σ win2 gap. Matches my measurements exactly. ✓
2. **README D4 row** — now "5 flag fails {Q2, N1, N5, S1, S2}, first Q2 tree `110` for `011`," which is what both my rerun and the author's stored log show. ✓
3. **Cross-references** — the wrong "two sections below" now cites "Inter-Pair Cancellation, Both Directions" by title, and all three fragile relative refs I flagged were converted to quoted titles; each verified to resolve. ✓
4. **N4/N5 payload assertions** — the `chk` task now asserts `res[22:0]` against nonzero NaN-row expectations, sign still never compared, generated-NaN rows (exp = 0) still class+quiet only; the README records the addition with its own mutation proof. I re-proved non-vacuity independently and more broadly than the README's record: corrupting N4's expectation to `7FC00002` fails **both** structures, and corrupting N5's to `7FE00001` also fails **both** structures — all four payload-comparison sites demonstrably execute and are sensitive. Indirect guard check: N1–N3 (exp = 0, actual payload 0x400000 ≠ 0) still pass, so the exp≠0 gate correctly bypasses rather than comparing against zero. Shipped bench: 3/3 harness green. ✓
5. **Notes** — 28 checks in both places, SB row present in the §12 table. ✓
6. **Four-rules scoping** — "order-mismatched *result-bits* reference … no more, no fewer (a flags-carrying reference fails on more: flags diverge on some bits-agreeing quadruples)". Matches my 1,369-vs-1,693 measurement. ✓
7. **Naive-model wording** — "never rounds an intermediate to binary32 (its three double-precision roundings are all it does)". ✓
8. **T2 row** — now states the tree-shaped `(a+b)+(c+d)` double-sum association, making the 1,562 count exactly reproducible. ✓

Re-verified after all edits: `run_all.sh ch10` 3/3; full-repo regression **95/95 cold** (my own run, exit 0); all 4 chapter listings still byte-identical contiguous slices of the shipped files.

Two residual cosmetic items, neither score-relevant, for the next touch of these files: (a) `tb_corners4.v`'s header comment (line 23) still says "1 = NaN expected (class + quiet bit checked, **bits printed only**)" — now contradicted by the in-task assertion the fix added (the in-task comment is correct); one line to update. (b) `wc -w` on the chapter gives **9,575**, not the 9,784 reported in the fix summary (and STATE.md's resume block still says 9,534 from writing time); trivial bookkeeping.

**Final score: 9 / 10 — fit to ship.** Every number independently confirmed in round 1; every defect from round 1 fixed and re-verified, with the one code change mutation-proved non-vacuous in both structures.
